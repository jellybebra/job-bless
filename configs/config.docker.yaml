web:
  host: 0.0.0.0
  port: 8080
  require_auth: true

browser:
  endpoint: ws://hh-browser:3000/hh

postgres:
  driver: sqlite
  sqlite_path: ./data/career_agent.db

llm:
  enabled: false
  connection: custom
  model: gemini-flash-latest

log:
  level: info

hh_autoscroller:
  search_url: https://hh.ru/search/vacancy?text=Python
  max_pages: 10
